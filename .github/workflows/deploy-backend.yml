name: Deploy Backend to EC2

on:
  push:
    branches:
      - 
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'

env:
  DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
  IMAGE_NAME: housescanner-backend
  EC2_HOST: ${{ secrets.EC2_HOST }}
  EC2_USER: ubuntu

jobs:
  build:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    outputs:
      sha_short: ${{ steps.vars.outputs.sha_short }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Get short commit SHA
        id: vars
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile.prod
          platforms: linux/amd64
          push: true
          tags: |
            ${{ env.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ steps.vars.outputs.sha_short }}
            ${{ env.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:latest
          cache-from: type=registry,ref=${{ env.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:buildcache,mode=max

      - name: Build Summary
        run: |
          echo "✅ Docker Image Built Successfully!"
          echo "📦 Image: ${{ env.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ steps.vars.outputs.sha_short }}"
          echo "🏷️  Tags: latest, ${{ steps.vars.outputs.sha_short }}"

  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Deploy to EC2 via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ env.EC2_HOST }}
          username: ${{ env.EC2_USER }}
          key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
          port: 22
          script: |
            cd /home/ubuntu/housescanner
            
            echo "🔄 Pulling latest image..."
            docker pull ${{ env.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:latest
            
            echo "🚀 Restarting backend service..."
            docker compose -f docker-compose.prod.yml up -d --no-deps --build backend
            
            echo "⏳ Waiting for health check..."
            timeout 60 sh -c 'until docker inspect --format="{{.State.Health.Status}}" housescanner-backend-1 | grep -q "healthy"; do sleep 2; done' || echo "⚠️  Health check timeout (service may still be starting)"
            
            echo ""
            echo "==================================="
            echo "📊 Container Status:"
            docker ps --filter name=housescanner-backend-1 --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
            
            echo ""
            echo "==================================="
            echo "📋 Recent Container Logs:"
            docker logs --tail 20 housescanner-backend-1
            
            echo ""
            echo "🧹 Cleaning up old images..."
            docker image prune -f

      - name: Deployment Summary
        run: |
          echo "🎉 Backend Deployment Complete!"
          echo "📦 Image: ${{ env.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ needs.build.outputs.sha_short }}"
          echo "🌐 Service: http://${{ env.EC2_HOST }}:3000"
          echo "🏷️  Commit: ${{ github.sha }}"
